# URL of the backend API
# For local development, this defaults to http://localhost:3000/api if not set
# For production (Vercel), set this in the Vercel Project Settings -> Environment Variables
VITE_API_URL=http://localhost:3000/api
